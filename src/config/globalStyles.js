import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    color: '#FFF', // Branco
    marginBottom: 20,
  },
  subtitle: {
    fontSize: 20,
    color: '#FFF', // Branco
    marginBottom: 5,
  },
  tagline: {
    fontSize: 16,
    color: '#FFF', // Branco
    opacity: 0.8,
  },
});